name: Build and Deploy
on:
  push:
    branches:
      - master
        
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello build workflow!"
      - uses: podkolzzzin/.github/workflows/base.yml